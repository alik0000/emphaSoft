export default {
    Loading(state) {
        return state.loading
    },
    ModalComponent(state) {
        return state.modalComponent
    },
    ModalVisible(state) {
        return state.modalVisible
    }
}